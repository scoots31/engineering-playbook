# Projects Registry

Maps project names to absolute paths. One row per project. Used by the framework
to resume a named project — "auto pilot on [name]" or `/auto-pilot [name]`.

| Name | Path |
|------|------|
