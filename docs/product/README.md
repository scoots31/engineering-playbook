# Product docs

Place product-specific PRDs, roadmaps, and metrics here **or** in your product repo’s preferred `docs/` layout. This folder exists as a placeholder when you want product docs inside the playbook repo (uncommon when the playbook is a submodule—usually product docs live in the parent repo).
